apiVersion: resourcemanager.stackit.crossplane.io/v1alpha1
kind: Folder
metadata:
  labels:
    testing.upbound.io/example-name: example-folder
  name: example-folder
spec:
  forProvider:
    name: example-folder
    parentContainerId: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
    ownerEmail: admin@example.com
  providerConfigRef:
    name: default
