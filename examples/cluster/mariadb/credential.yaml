apiVersion: mariadb.stackit.crossplane.io/v1alpha1
kind: Credential
metadata:
  labels:
    testing.upbound.io/example-name: example-mariadb-credential
  name: example-mariadb-credential
spec:
  forProvider:
    projectId: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
    instanceIdSelector:
      matchLabels:
        testing.upbound.io/example-name: example-mariadb
  providerConfigRef:
    name: default
