---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: arearoutes.network.stackit.m.crossplane.io
spec:
  group: network.stackit.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - stackit
    kind: AreaRoute
    listKind: AreaRouteList
    plural: arearoutes
    singular: arearoute
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AreaRoute is the Schema for the AreaRoutes API. Network area
          route resource schema. Must have a
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AreaRouteSpec defines the desired state of AreaRoute
            properties:
              forProvider:
                properties:
                  destination:
                    description: (Attributes) Destination of the route. (see below
                      for nested schema)
                    properties:
                      type:
                        description: |-
                          (String) CIDRV type. Possible values are: cidrv4, cidrv6. Only cidrv4 is supported currently.
                          CIDRV type. Possible values are: `cidrv4`, `cidrv6`. Only `cidrv4` is supported currently.
                        type: string
                      value:
                        description: |-
                          (String) An CIDR string.
                          An CIDR string.
                        type: string
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      value string pairs which can be attached to a resource container
                      Labels are key-value string pairs which can be attached to a resource container
                    type: object
                    x-kubernetes-map-type: granular
                  networkAreaId:
                    description: |-
                      (String) The network area ID to which the network area route is associated.
                      The network area ID to which the network area route is associated.
                    type: string
                  networkAreaIdRef:
                    description: Reference to a Area in network to populate networkAreaId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkAreaIdSelector:
                    description: Selector for a Area in network to populate networkAreaId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  nextHop:
                    description: (Attributes) Next hop destination. (see below for
                      nested schema)
                    properties:
                      type:
                        description: |-
                          (String) CIDRV type. Possible values are: cidrv4, cidrv6. Only cidrv4 is supported currently.
                          Type of the next hop. Possible values are: `blackhole`, `internet`, `ipv4`, `ipv6`. Only `ipv4` supported currently.
                        type: string
                      value:
                        description: |-
                          (String) An CIDR string.
                          Either IPv4 or IPv6 (not set for blackhole and internet). Only IPv4 supported currently.
                        type: string
                    type: object
                  organizationId:
                    description: |-
                      (String) STACKIT organization ID to which the network area is associated.
                      STACKIT organization ID to which the network area is associated.
                    type: string
                  region:
                    description: |-
                      (String) The resource region. If not defined, the provider region is used.
                      The resource region. If not defined, the provider region is used.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  destination:
                    description: (Attributes) Destination of the route. (see below
                      for nested schema)
                    properties:
                      type:
                        description: |-
                          (String) CIDRV type. Possible values are: cidrv4, cidrv6. Only cidrv4 is supported currently.
                          CIDRV type. Possible values are: `cidrv4`, `cidrv6`. Only `cidrv4` is supported currently.
                        type: string
                      value:
                        description: |-
                          (String) An CIDR string.
                          An CIDR string.
                        type: string
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      value string pairs which can be attached to a resource container
                      Labels are key-value string pairs which can be attached to a resource container
                    type: object
                    x-kubernetes-map-type: granular
                  networkAreaId:
                    description: |-
                      (String) The network area ID to which the network area route is associated.
                      The network area ID to which the network area route is associated.
                    type: string
                  networkAreaIdRef:
                    description: Reference to a Area in network to populate networkAreaId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkAreaIdSelector:
                    description: Selector for a Area in network to populate networkAreaId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  nextHop:
                    description: (Attributes) Next hop destination. (see below for
                      nested schema)
                    properties:
                      type:
                        description: |-
                          (String) CIDRV type. Possible values are: cidrv4, cidrv6. Only cidrv4 is supported currently.
                          Type of the next hop. Possible values are: `blackhole`, `internet`, `ipv4`, `ipv6`. Only `ipv4` supported currently.
                        type: string
                      value:
                        description: |-
                          (String) An CIDR string.
                          Either IPv4 or IPv6 (not set for blackhole and internet). Only IPv4 supported currently.
                        type: string
                    type: object
                  organizationId:
                    description: |-
                      (String) STACKIT organization ID to which the network area is associated.
                      STACKIT organization ID to which the network area is associated.
                    type: string
                  region:
                    description: |-
                      (String) The resource region. If not defined, the provider region is used.
                      The resource region. If not defined, the provider region is used.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.destination is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.destination)
                || (has(self.initProvider) && has(self.initProvider.destination))'
            - message: spec.forProvider.nextHop is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.nextHop)
                || (has(self.initProvider) && has(self.initProvider.nextHop))'
            - message: spec.forProvider.organizationId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.organizationId)
                || (has(self.initProvider) && has(self.initProvider.organizationId))'
          status:
            description: AreaRouteStatus defines the observed state of AreaRoute.
            properties:
              atProvider:
                properties:
                  destination:
                    description: (Attributes) Destination of the route. (see below
                      for nested schema)
                    properties:
                      type:
                        description: |-
                          (String) CIDRV type. Possible values are: cidrv4, cidrv6. Only cidrv4 is supported currently.
                          CIDRV type. Possible values are: `cidrv4`, `cidrv6`. Only `cidrv4` is supported currently.
                        type: string
                      value:
                        description: |-
                          (String) An CIDR string.
                          An CIDR string.
                        type: string
                    type: object
                  id:
                    description: It is structured as "organization_id,network_area_id,region,network_area_route_id".
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      value string pairs which can be attached to a resource container
                      Labels are key-value string pairs which can be attached to a resource container
                    type: object
                    x-kubernetes-map-type: granular
                  networkAreaId:
                    description: |-
                      (String) The network area ID to which the network area route is associated.
                      The network area ID to which the network area route is associated.
                    type: string
                  networkAreaRouteId:
                    description: |-
                      (String) The network area route ID.
                      The network area route ID.
                    type: string
                  nextHop:
                    description: (Attributes) Next hop destination. (see below for
                      nested schema)
                    properties:
                      type:
                        description: |-
                          (String) CIDRV type. Possible values are: cidrv4, cidrv6. Only cidrv4 is supported currently.
                          Type of the next hop. Possible values are: `blackhole`, `internet`, `ipv4`, `ipv6`. Only `ipv4` supported currently.
                        type: string
                      value:
                        description: |-
                          (String) An CIDR string.
                          Either IPv4 or IPv6 (not set for blackhole and internet). Only IPv4 supported currently.
                        type: string
                    type: object
                  organizationId:
                    description: |-
                      (String) STACKIT organization ID to which the network area is associated.
                      STACKIT organization ID to which the network area is associated.
                    type: string
                  region:
                    description: |-
                      (String) The resource region. If not defined, the provider region is used.
                      The resource region. If not defined, the provider region is used.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
