---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: distributions.cdn.stackit.crossplane.io
spec:
  group: cdn.stackit.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - stackit
    kind: Distribution
    listKind: DistributionList
    plural: distributions
    singular: distribution
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Distribution is the Schema for the Distributions API. CDN distribution
          data source schema. ~> This resource is in beta and may be subject to breaking
          changes in the future. Use with caution. See our guide https://registry.io/providers/stackitcloud/stackit/latest/docs/guides/opting_into_beta_resources
          for how to opt-in to use beta resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DistributionSpec defines the desired state of Distribution
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  config:
                    description: (Attributes) The distribution configuration (see
                      below for nested schema)
                    properties:
                      backend:
                        description: (Attributes) The configured backend for the distribution
                          (see below for nested schema)
                        properties:
                          bucketUrl:
                            description: |-
                              (String) The URL of the bucket (e.g. https://s3.example.com). Required if type is 'bucket'.
                              The URL of the bucket (e.g. https://s3.example.com). Required if type is 'bucket'.
                            type: string
                          credentials:
                            description: (Attributes) The credentials for the bucket.
                              Required if type is 'bucket'. (see below for nested
                              schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  (String, Sensitive) The access key for the bucket. Required if type is 'bucket'.
                                  The access key for the bucket. Required if type is 'bucket'.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretAccessKeySecretRef:
                                description: |-
                                  (String, Sensitive) The access key for the bucket. Required if type is 'bucket'.
                                  The access key for the bucket. Required if type is 'bucket'.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          geofencing:
                            additionalProperties:
                              items:
                                type: string
                              type: array
                            description: |-
                              (Map of List of String) The configured type http to configure countries where content is allowed. A map of URLs to a list of countries
                              The configured type http to configure countries where content is allowed. A map of URLs to a list of countries
                            type: object
                          originRequestHeaders:
                            additionalProperties:
                              type: string
                            description: |-
                              (Map of String) The configured type http origin request headers for the backend
                              The configured type http origin request headers for the backend
                            type: object
                            x-kubernetes-map-type: granular
                          originUrl:
                            description: |-
                              (String) The configured backend type http for the distribution
                              The configured backend type http for the distribution
                            type: string
                          region:
                            description: |-
                              (String) The region where the bucket is hosted. Required if type is 'bucket'.
                              The region where the bucket is hosted. Required if type is 'bucket'.
                            type: string
                          type:
                            description: |-
                              (String) The configured backend type. Possible values are: http, bucket.
                              The configured backend type. Possible values are: `http`, `bucket`.
                            type: string
                        type: object
                      blockedCountries:
                        description: |-
                          (List of String) The configured countries where distribution of content is blocked
                          The configured countries where distribution of content is blocked
                        items:
                          type: string
                        type: array
                      optimizer:
                        description: (Attributes) Configuration for the Image Optimizer.
                          This is a paid feature that automatically optimizes images
                          to reduce their file size for faster delivery, leading to
                          improved website performance and a better user experience.
                          (see below for nested schema)
                        properties:
                          enabled:
                            description: (Boolean)
                            type: boolean
                        type: object
                      regions:
                        description: |-
                          (List of String) The configured regions where content will be hosted
                          The configured regions where content will be hosted
                        items:
                          type: string
                        type: array
                    type: object
                  projectId:
                    description: |-
                      (String) STACKIT project ID associated with the distribution
                      STACKIT project ID associated with the distribution
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  config:
                    description: (Attributes) The distribution configuration (see
                      below for nested schema)
                    properties:
                      backend:
                        description: (Attributes) The configured backend for the distribution
                          (see below for nested schema)
                        properties:
                          bucketUrl:
                            description: |-
                              (String) The URL of the bucket (e.g. https://s3.example.com). Required if type is 'bucket'.
                              The URL of the bucket (e.g. https://s3.example.com). Required if type is 'bucket'.
                            type: string
                          credentials:
                            description: (Attributes) The credentials for the bucket.
                              Required if type is 'bucket'. (see below for nested
                              schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  (String, Sensitive) The access key for the bucket. Required if type is 'bucket'.
                                  The access key for the bucket. Required if type is 'bucket'.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretAccessKeySecretRef:
                                description: |-
                                  (String, Sensitive) The access key for the bucket. Required if type is 'bucket'.
                                  The access key for the bucket. Required if type is 'bucket'.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            required:
                            - accessKeyIdSecretRef
                            - secretAccessKeySecretRef
                            type: object
                          geofencing:
                            additionalProperties:
                              items:
                                type: string
                              type: array
                            description: |-
                              (Map of List of String) The configured type http to configure countries where content is allowed. A map of URLs to a list of countries
                              The configured type http to configure countries where content is allowed. A map of URLs to a list of countries
                            type: object
                          originRequestHeaders:
                            additionalProperties:
                              type: string
                            description: |-
                              (Map of String) The configured type http origin request headers for the backend
                              The configured type http origin request headers for the backend
                            type: object
                            x-kubernetes-map-type: granular
                          originUrl:
                            description: |-
                              (String) The configured backend type http for the distribution
                              The configured backend type http for the distribution
                            type: string
                          region:
                            description: |-
                              (String) The region where the bucket is hosted. Required if type is 'bucket'.
                              The region where the bucket is hosted. Required if type is 'bucket'.
                            type: string
                          type:
                            description: |-
                              (String) The configured backend type. Possible values are: http, bucket.
                              The configured backend type. Possible values are: `http`, `bucket`.
                            type: string
                        type: object
                      blockedCountries:
                        description: |-
                          (List of String) The configured countries where distribution of content is blocked
                          The configured countries where distribution of content is blocked
                        items:
                          type: string
                        type: array
                      optimizer:
                        description: (Attributes) Configuration for the Image Optimizer.
                          This is a paid feature that automatically optimizes images
                          to reduce their file size for faster delivery, leading to
                          improved website performance and a better user experience.
                          (see below for nested schema)
                        properties:
                          enabled:
                            description: (Boolean)
                            type: boolean
                        type: object
                      regions:
                        description: |-
                          (List of String) The configured regions where content will be hosted
                          The configured regions where content will be hosted
                        items:
                          type: string
                        type: array
                    type: object
                  projectId:
                    description: |-
                      (String) STACKIT project ID associated with the distribution
                      STACKIT project ID associated with the distribution
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.config is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.config)
                || (has(self.initProvider) && has(self.initProvider.config))'
            - message: spec.forProvider.projectId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.projectId)
                || (has(self.initProvider) && has(self.initProvider.projectId))'
          status:
            description: DistributionStatus defines the observed state of Distribution.
            properties:
              atProvider:
                properties:
                  config:
                    description: (Attributes) The distribution configuration (see
                      below for nested schema)
                    properties:
                      backend:
                        description: (Attributes) The configured backend for the distribution
                          (see below for nested schema)
                        properties:
                          bucketUrl:
                            description: |-
                              (String) The URL of the bucket (e.g. https://s3.example.com). Required if type is 'bucket'.
                              The URL of the bucket (e.g. https://s3.example.com). Required if type is 'bucket'.
                            type: string
                          credentials:
                            description: (Attributes) The credentials for the bucket.
                              Required if type is 'bucket'. (see below for nested
                              schema)
                            properties:
                              accessKeyIdSecretRef:
                                description: |-
                                  (String, Sensitive) The access key for the bucket. Required if type is 'bucket'.
                                  The access key for the bucket. Required if type is 'bucket'.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              secretAccessKeySecretRef:
                                description: |-
                                  (String, Sensitive) The access key for the bucket. Required if type is 'bucket'.
                                  The access key for the bucket. Required if type is 'bucket'.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          geofencing:
                            additionalProperties:
                              items:
                                type: string
                              type: array
                            description: |-
                              (Map of List of String) The configured type http to configure countries where content is allowed. A map of URLs to a list of countries
                              The configured type http to configure countries where content is allowed. A map of URLs to a list of countries
                            type: object
                          originRequestHeaders:
                            additionalProperties:
                              type: string
                            description: |-
                              (Map of String) The configured type http origin request headers for the backend
                              The configured type http origin request headers for the backend
                            type: object
                            x-kubernetes-map-type: granular
                          originUrl:
                            description: |-
                              (String) The configured backend type http for the distribution
                              The configured backend type http for the distribution
                            type: string
                          region:
                            description: |-
                              (String) The region where the bucket is hosted. Required if type is 'bucket'.
                              The region where the bucket is hosted. Required if type is 'bucket'.
                            type: string
                          type:
                            description: |-
                              (String) The configured backend type. Possible values are: http, bucket.
                              The configured backend type. Possible values are: `http`, `bucket`.
                            type: string
                        type: object
                      blockedCountries:
                        description: |-
                          (List of String) The configured countries where distribution of content is blocked
                          The configured countries where distribution of content is blocked
                        items:
                          type: string
                        type: array
                      optimizer:
                        description: (Attributes) Configuration for the Image Optimizer.
                          This is a paid feature that automatically optimizes images
                          to reduce their file size for faster delivery, leading to
                          improved website performance and a better user experience.
                          (see below for nested schema)
                        properties:
                          enabled:
                            description: (Boolean)
                            type: boolean
                        type: object
                      regions:
                        description: |-
                          (List of String) The configured regions where content will be hosted
                          The configured regions where content will be hosted
                        items:
                          type: string
                        type: array
                    type: object
                  createdAt:
                    description: |-
                      (String) Time when the distribution was created
                      Time when the distribution was created
                    type: string
                  distributionId:
                    description: |-
                      (String) CDN distribution ID
                      CDN distribution ID
                    type: string
                  domains:
                    description: (Attributes List) List of configured domains for
                      the distribution (see below for nested schema)
                    items:
                      properties:
                        errors:
                          description: |-
                            (List of String) List of distribution errors
                            List of domain errors
                          items:
                            type: string
                          type: array
                        name:
                          description: |-
                            (String) The name of the domain
                            The name of the domain
                          type: string
                        status:
                          description: |-
                            (String) Status of the distribution
                            The status of the domain
                          type: string
                        type:
                          description: |-
                            (String) The configured backend type. Possible values are: http, bucket.
                            The type of the domain. Each distribution has one domain of type "managed", and domains of type "custom" may be additionally created by the user
                          type: string
                      type: object
                    type: array
                  errors:
                    description: |-
                      (List of String) List of distribution errors
                      List of distribution errors
                    items:
                      type: string
                    type: array
                  id:
                    description: It is structured as "project_id,distribution_id".
                    type: string
                  projectId:
                    description: |-
                      (String) STACKIT project ID associated with the distribution
                      STACKIT project ID associated with the distribution
                    type: string
                  status:
                    description: |-
                      (String) Status of the distribution
                      Status of the distribution
                    type: string
                  updatedAt:
                    description: |-
                      (String) Time when the distribution was last updated
                      Time when the distribution was last updated
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
