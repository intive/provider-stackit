---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: servers.compute.stackit.m.crossplane.io
spec:
  group: compute.stackit.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - stackit
    kind: Server
    listKind: ServerList
    plural: servers
    singular: server
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Server is the Schema for the Servers API. Server resource schema.
          Must have a region specified in the provider configuration. Example Usage
          With key pair
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerSpec defines the desired state of Server
            properties:
              forProvider:
                properties:
                  affinityGroup:
                    description: |-
                      (String) The affinity group the server is assigned to.
                      The affinity group the server is assigned to.
                    type: string
                  availabilityZone:
                    description: |-
                      (String) The availability zone of the server.
                      The availability zone of the server.
                    type: string
                  bootVolume:
                    description: (Attributes) The boot volume for the server (see
                      below for nested schema)
                    properties:
                      deleteOnTermination:
                        description: |-
                          (Boolean) Delete the volume during the termination of the server. Only allowed when source_type is image.
                          Delete the volume during the termination of the server. Only allowed when `source_type` is `image`.
                        type: boolean
                      performanceClass:
                        description: |-
                          (String) The performance class of the server.
                          The performance class of the server.
                        type: string
                      size:
                        description: |-
                          (Number) The size of the boot volume in GB. Must be provided when source_type is image.
                          The size of the boot volume in GB. Must be provided when `source_type` is `image`.
                        type: number
                      sourceId:
                        description: |-
                          (String) The ID of the source, either image ID or volume ID
                          The ID of the source, either image ID or volume ID
                        type: string
                      sourceType:
                        description: |-
                          (String) The type of the source. Possible values are: volume, image.
                          The type of the source. Possible values are: `volume`, `image`.
                        type: string
                    type: object
                  desiredStatus:
                    description: |-
                      (String) The desired status of the server resource. Possible values are: active, inactive, deallocated.
                      The desired status of the server resource. Possible values are: `active`, `inactive`, `deallocated`.
                    type: string
                  imageId:
                    description: |-
                      (String) The image ID to be used for an ephemeral disk on the server.
                      The image ID to be used for an ephemeral disk on the server.
                    type: string
                  imageIdRef:
                    description: Reference to a Image in compute to populate imageId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  imageIdSelector:
                    description: Selector for a Image in compute to populate imageId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  keypairName:
                    description: |-
                      (String) The name of the keypair used during server creation.
                      The name of the keypair used during server creation.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      value string pairs which can be attached to a resource container
                      Labels are key-value string pairs which can be attached to a resource container
                    type: object
                    x-kubernetes-map-type: granular
                  machineType:
                    description: |-
                      (String) Name of the type of the machine for the server. Possible values are documented in Virtual machine flavors
                      Name of the type of the machine for the server. Possible values are documented in [Virtual machine flavors](https://docs.stackit.cloud/products/compute-engine/server/basics/machine-types/)
                    type: string
                  name:
                    description: |-
                      (String) The name of the server.
                      The name of the server.
                    type: string
                  networkInterfaces:
                    description: |-
                      )creating servers. Still marked as optional in the schema to not introduce breaking changes. There will be a migration path for this field soon.
                      The IDs of network interfaces which should be attached to the server. Updating it will recreate the server. **Required when (re-)creating servers. Still marked as optional in the schema to not introduce breaking changes. There will be a migration path for this field soon.**
                    items:
                      type: string
                    type: array
                  projectId:
                    description: |-
                      (String) STACKIT project ID to which the server is associated.
                      STACKIT project ID to which the server is associated.
                    type: string
                  region:
                    description: |-
                      (String) The resource region. If not defined, the provider region is used.
                      The resource region. If not defined, the provider region is used.
                    type: string
                  userData:
                    description: |-
                      init to the server.
                      User data that is passed via cloud-init to the server.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  affinityGroup:
                    description: |-
                      (String) The affinity group the server is assigned to.
                      The affinity group the server is assigned to.
                    type: string
                  availabilityZone:
                    description: |-
                      (String) The availability zone of the server.
                      The availability zone of the server.
                    type: string
                  bootVolume:
                    description: (Attributes) The boot volume for the server (see
                      below for nested schema)
                    properties:
                      deleteOnTermination:
                        description: |-
                          (Boolean) Delete the volume during the termination of the server. Only allowed when source_type is image.
                          Delete the volume during the termination of the server. Only allowed when `source_type` is `image`.
                        type: boolean
                      performanceClass:
                        description: |-
                          (String) The performance class of the server.
                          The performance class of the server.
                        type: string
                      size:
                        description: |-
                          (Number) The size of the boot volume in GB. Must be provided when source_type is image.
                          The size of the boot volume in GB. Must be provided when `source_type` is `image`.
                        type: number
                      sourceId:
                        description: |-
                          (String) The ID of the source, either image ID or volume ID
                          The ID of the source, either image ID or volume ID
                        type: string
                      sourceType:
                        description: |-
                          (String) The type of the source. Possible values are: volume, image.
                          The type of the source. Possible values are: `volume`, `image`.
                        type: string
                    type: object
                  desiredStatus:
                    description: |-
                      (String) The desired status of the server resource. Possible values are: active, inactive, deallocated.
                      The desired status of the server resource. Possible values are: `active`, `inactive`, `deallocated`.
                    type: string
                  imageId:
                    description: |-
                      (String) The image ID to be used for an ephemeral disk on the server.
                      The image ID to be used for an ephemeral disk on the server.
                    type: string
                  imageIdRef:
                    description: Reference to a Image in compute to populate imageId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  imageIdSelector:
                    description: Selector for a Image in compute to populate imageId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  keypairName:
                    description: |-
                      (String) The name of the keypair used during server creation.
                      The name of the keypair used during server creation.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      value string pairs which can be attached to a resource container
                      Labels are key-value string pairs which can be attached to a resource container
                    type: object
                    x-kubernetes-map-type: granular
                  machineType:
                    description: |-
                      (String) Name of the type of the machine for the server. Possible values are documented in Virtual machine flavors
                      Name of the type of the machine for the server. Possible values are documented in [Virtual machine flavors](https://docs.stackit.cloud/products/compute-engine/server/basics/machine-types/)
                    type: string
                  name:
                    description: |-
                      (String) The name of the server.
                      The name of the server.
                    type: string
                  networkInterfaces:
                    description: |-
                      )creating servers. Still marked as optional in the schema to not introduce breaking changes. There will be a migration path for this field soon.
                      The IDs of network interfaces which should be attached to the server. Updating it will recreate the server. **Required when (re-)creating servers. Still marked as optional in the schema to not introduce breaking changes. There will be a migration path for this field soon.**
                    items:
                      type: string
                    type: array
                  projectId:
                    description: |-
                      (String) STACKIT project ID to which the server is associated.
                      STACKIT project ID to which the server is associated.
                    type: string
                  region:
                    description: |-
                      (String) The resource region. If not defined, the provider region is used.
                      The resource region. If not defined, the provider region is used.
                    type: string
                  userData:
                    description: |-
                      init to the server.
                      User data that is passed via cloud-init to the server.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.machineType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.machineType)
                || (has(self.initProvider) && has(self.initProvider.machineType))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.projectId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.projectId)
                || (has(self.initProvider) && has(self.initProvider.projectId))'
          status:
            description: ServerStatus defines the observed state of Server.
            properties:
              atProvider:
                properties:
                  affinityGroup:
                    description: |-
                      (String) The affinity group the server is assigned to.
                      The affinity group the server is assigned to.
                    type: string
                  availabilityZone:
                    description: |-
                      (String) The availability zone of the server.
                      The availability zone of the server.
                    type: string
                  bootVolume:
                    description: (Attributes) The boot volume for the server (see
                      below for nested schema)
                    properties:
                      deleteOnTermination:
                        description: |-
                          (Boolean) Delete the volume during the termination of the server. Only allowed when source_type is image.
                          Delete the volume during the termination of the server. Only allowed when `source_type` is `image`.
                        type: boolean
                      id:
                        description: |-
                          It is structured as "project_id,region,server_id".
                          The ID of the boot volume
                        type: string
                      performanceClass:
                        description: |-
                          (String) The performance class of the server.
                          The performance class of the server.
                        type: string
                      size:
                        description: |-
                          (Number) The size of the boot volume in GB. Must be provided when source_type is image.
                          The size of the boot volume in GB. Must be provided when `source_type` is `image`.
                        type: number
                      sourceId:
                        description: |-
                          (String) The ID of the source, either image ID or volume ID
                          The ID of the source, either image ID or volume ID
                        type: string
                      sourceType:
                        description: |-
                          (String) The type of the source. Possible values are: volume, image.
                          The type of the source. Possible values are: `volume`, `image`.
                        type: string
                    type: object
                  createdAt:
                    description: |-
                      time when the server was created
                      Date-time when the server was created
                    type: string
                  desiredStatus:
                    description: |-
                      (String) The desired status of the server resource. Possible values are: active, inactive, deallocated.
                      The desired status of the server resource. Possible values are: `active`, `inactive`, `deallocated`.
                    type: string
                  id:
                    description: It is structured as "project_id,region,server_id".
                    type: string
                  imageId:
                    description: |-
                      (String) The image ID to be used for an ephemeral disk on the server.
                      The image ID to be used for an ephemeral disk on the server.
                    type: string
                  keypairName:
                    description: |-
                      (String) The name of the keypair used during server creation.
                      The name of the keypair used during server creation.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      value string pairs which can be attached to a resource container
                      Labels are key-value string pairs which can be attached to a resource container
                    type: object
                    x-kubernetes-map-type: granular
                  launchedAt:
                    description: |-
                      time when the server was launched
                      Date-time when the server was launched
                    type: string
                  machineType:
                    description: |-
                      (String) Name of the type of the machine for the server. Possible values are documented in Virtual machine flavors
                      Name of the type of the machine for the server. Possible values are documented in [Virtual machine flavors](https://docs.stackit.cloud/products/compute-engine/server/basics/machine-types/)
                    type: string
                  name:
                    description: |-
                      (String) The name of the server.
                      The name of the server.
                    type: string
                  networkInterfaces:
                    description: |-
                      )creating servers. Still marked as optional in the schema to not introduce breaking changes. There will be a migration path for this field soon.
                      The IDs of network interfaces which should be attached to the server. Updating it will recreate the server. **Required when (re-)creating servers. Still marked as optional in the schema to not introduce breaking changes. There will be a migration path for this field soon.**
                    items:
                      type: string
                    type: array
                  projectId:
                    description: |-
                      (String) STACKIT project ID to which the server is associated.
                      STACKIT project ID to which the server is associated.
                    type: string
                  region:
                    description: |-
                      (String) The resource region. If not defined, the provider region is used.
                      The resource region. If not defined, the provider region is used.
                    type: string
                  serverId:
                    description: |-
                      (String) The server ID.
                      The server ID.
                    type: string
                  updatedAt:
                    description: |-
                      time when the server was updated
                      Date-time when the server was updated
                    type: string
                  userData:
                    description: |-
                      init to the server.
                      User data that is passed via cloud-init to the server.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
