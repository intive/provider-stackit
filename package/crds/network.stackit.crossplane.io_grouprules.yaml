---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: grouprules.network.stackit.crossplane.io
spec:
  group: network.stackit.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - stackit
    kind: GroupRule
    listKind: GroupRuleList
    plural: grouprules
    singular: grouprule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GroupRule is the Schema for the GroupRules API. Security group
          rule resource schema. Must have a region specified in the provider configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GroupRuleSpec defines the desired state of GroupRule
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  description:
                    description: |-
                      (String) The rule description.
                      The rule description.
                    type: string
                  direction:
                    description: |-
                      (String) The direction of the traffic which the rule should match. Some of the possible values are: Possible values are: ingress, egress.
                      The direction of the traffic which the rule should match. Some of the possible values are: Possible values are: `ingress`, `egress`.
                    type: string
                  etherType:
                    description: |-
                      (String) The ethertype which the rule should match.
                      The ethertype which the rule should match.
                    type: string
                  icmpParameters:
                    description: (Attributes) ICMP Parameters. These parameters should
                      only be provided if the protocol is ICMP. (see below for nested
                      schema)
                    properties:
                      code:
                        description: |-
                          (Number) ICMP code. Can be set if the protocol is ICMP.
                          ICMP code. Can be set if the protocol is ICMP.
                        type: number
                      type:
                        description: |-
                          (Number) ICMP type. Can be set if the protocol is ICMP.
                          ICMP type. Can be set if the protocol is ICMP.
                        type: number
                    type: object
                  ipRange:
                    description: |-
                      (String) The remote IP range which the rule should match.
                      The remote IP range which the rule should match.
                    type: string
                  portRange:
                    description: (Attributes) The range of ports. This should only
                      be provided if the protocol is not ICMP. (see below for nested
                      schema)
                    properties:
                      max:
                        description: |-
                          (Number) The maximum port number. Should be greater or equal to the minimum.
                          The maximum port number. Should be greater or equal to the minimum.
                        type: number
                      min:
                        description: |-
                          (Number) The minimum port number. Should be less or equal to the maximum.
                          The minimum port number. Should be less or equal to the maximum.
                        type: number
                    type: object
                  projectId:
                    description: |-
                      (String) STACKIT project ID to which the security group rule is associated.
                      STACKIT project ID to which the security group rule is associated.
                    type: string
                  protocol:
                    description: (Attributes) The internet protocol which the rule
                      should match. (see below for nested schema)
                    properties:
                      name:
                        description: |-
                          encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp.
                          The protocol name which the rule should match. Either `name` or `number` must be provided. Possible values are: `ah`, `dccp`, `egp`, `esp`, `gre`, `icmp`, `igmp`, `ipip`, `ipv6-encap`, `ipv6-frag`, `ipv6-icmp`, `ipv6-nonxt`, `ipv6-opts`, `ipv6-route`, `ospf`, `pgm`, `rsvp`, `sctp`, `tcp`, `udp`, `udplite`, `vrrp`.
                        type: string
                      number:
                        description: |-
                          (Number) The protocol number which the rule should match. Either name or number must be provided.
                          The protocol number which the rule should match. Either `name` or `number` must be provided.
                        type: number
                    type: object
                  region:
                    description: |-
                      (String) The resource region. If not defined, the provider region is used.
                      The resource region. If not defined, the provider region is used.
                    type: string
                  remoteSecurityGroupId:
                    description: |-
                      (String) The remote security group which the rule should match.
                      The remote security group which the rule should match.
                    type: string
                  securityGroupId:
                    description: |-
                      (String) The security group ID.
                      The security group ID.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a Group in network to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a Group in network to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  description:
                    description: |-
                      (String) The rule description.
                      The rule description.
                    type: string
                  direction:
                    description: |-
                      (String) The direction of the traffic which the rule should match. Some of the possible values are: Possible values are: ingress, egress.
                      The direction of the traffic which the rule should match. Some of the possible values are: Possible values are: `ingress`, `egress`.
                    type: string
                  etherType:
                    description: |-
                      (String) The ethertype which the rule should match.
                      The ethertype which the rule should match.
                    type: string
                  icmpParameters:
                    description: (Attributes) ICMP Parameters. These parameters should
                      only be provided if the protocol is ICMP. (see below for nested
                      schema)
                    properties:
                      code:
                        description: |-
                          (Number) ICMP code. Can be set if the protocol is ICMP.
                          ICMP code. Can be set if the protocol is ICMP.
                        type: number
                      type:
                        description: |-
                          (Number) ICMP type. Can be set if the protocol is ICMP.
                          ICMP type. Can be set if the protocol is ICMP.
                        type: number
                    type: object
                  ipRange:
                    description: |-
                      (String) The remote IP range which the rule should match.
                      The remote IP range which the rule should match.
                    type: string
                  portRange:
                    description: (Attributes) The range of ports. This should only
                      be provided if the protocol is not ICMP. (see below for nested
                      schema)
                    properties:
                      max:
                        description: |-
                          (Number) The maximum port number. Should be greater or equal to the minimum.
                          The maximum port number. Should be greater or equal to the minimum.
                        type: number
                      min:
                        description: |-
                          (Number) The minimum port number. Should be less or equal to the maximum.
                          The minimum port number. Should be less or equal to the maximum.
                        type: number
                    type: object
                  projectId:
                    description: |-
                      (String) STACKIT project ID to which the security group rule is associated.
                      STACKIT project ID to which the security group rule is associated.
                    type: string
                  protocol:
                    description: (Attributes) The internet protocol which the rule
                      should match. (see below for nested schema)
                    properties:
                      name:
                        description: |-
                          encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp.
                          The protocol name which the rule should match. Either `name` or `number` must be provided. Possible values are: `ah`, `dccp`, `egp`, `esp`, `gre`, `icmp`, `igmp`, `ipip`, `ipv6-encap`, `ipv6-frag`, `ipv6-icmp`, `ipv6-nonxt`, `ipv6-opts`, `ipv6-route`, `ospf`, `pgm`, `rsvp`, `sctp`, `tcp`, `udp`, `udplite`, `vrrp`.
                        type: string
                      number:
                        description: |-
                          (Number) The protocol number which the rule should match. Either name or number must be provided.
                          The protocol number which the rule should match. Either `name` or `number` must be provided.
                        type: number
                    type: object
                  region:
                    description: |-
                      (String) The resource region. If not defined, the provider region is used.
                      The resource region. If not defined, the provider region is used.
                    type: string
                  remoteSecurityGroupId:
                    description: |-
                      (String) The remote security group which the rule should match.
                      The remote security group which the rule should match.
                    type: string
                  securityGroupId:
                    description: |-
                      (String) The security group ID.
                      The security group ID.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a Group in network to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a Group in network to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.direction is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.direction)
                || (has(self.initProvider) && has(self.initProvider.direction))'
            - message: spec.forProvider.projectId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.projectId)
                || (has(self.initProvider) && has(self.initProvider.projectId))'
          status:
            description: GroupRuleStatus defines the observed state of GroupRule.
            properties:
              atProvider:
                properties:
                  description:
                    description: |-
                      (String) The rule description.
                      The rule description.
                    type: string
                  direction:
                    description: |-
                      (String) The direction of the traffic which the rule should match. Some of the possible values are: Possible values are: ingress, egress.
                      The direction of the traffic which the rule should match. Some of the possible values are: Possible values are: `ingress`, `egress`.
                    type: string
                  etherType:
                    description: |-
                      (String) The ethertype which the rule should match.
                      The ethertype which the rule should match.
                    type: string
                  icmpParameters:
                    description: (Attributes) ICMP Parameters. These parameters should
                      only be provided if the protocol is ICMP. (see below for nested
                      schema)
                    properties:
                      code:
                        description: |-
                          (Number) ICMP code. Can be set if the protocol is ICMP.
                          ICMP code. Can be set if the protocol is ICMP.
                        type: number
                      type:
                        description: |-
                          (Number) ICMP type. Can be set if the protocol is ICMP.
                          ICMP type. Can be set if the protocol is ICMP.
                        type: number
                    type: object
                  id:
                    description: It is structured as "project_id,region,security_group_id,security_group_rule_id".
                    type: string
                  ipRange:
                    description: |-
                      (String) The remote IP range which the rule should match.
                      The remote IP range which the rule should match.
                    type: string
                  portRange:
                    description: (Attributes) The range of ports. This should only
                      be provided if the protocol is not ICMP. (see below for nested
                      schema)
                    properties:
                      max:
                        description: |-
                          (Number) The maximum port number. Should be greater or equal to the minimum.
                          The maximum port number. Should be greater or equal to the minimum.
                        type: number
                      min:
                        description: |-
                          (Number) The minimum port number. Should be less or equal to the maximum.
                          The minimum port number. Should be less or equal to the maximum.
                        type: number
                    type: object
                  projectId:
                    description: |-
                      (String) STACKIT project ID to which the security group rule is associated.
                      STACKIT project ID to which the security group rule is associated.
                    type: string
                  protocol:
                    description: (Attributes) The internet protocol which the rule
                      should match. (see below for nested schema)
                    properties:
                      name:
                        description: |-
                          encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp.
                          The protocol name which the rule should match. Either `name` or `number` must be provided. Possible values are: `ah`, `dccp`, `egp`, `esp`, `gre`, `icmp`, `igmp`, `ipip`, `ipv6-encap`, `ipv6-frag`, `ipv6-icmp`, `ipv6-nonxt`, `ipv6-opts`, `ipv6-route`, `ospf`, `pgm`, `rsvp`, `sctp`, `tcp`, `udp`, `udplite`, `vrrp`.
                        type: string
                      number:
                        description: |-
                          (Number) The protocol number which the rule should match. Either name or number must be provided.
                          The protocol number which the rule should match. Either `name` or `number` must be provided.
                        type: number
                    type: object
                  region:
                    description: |-
                      (String) The resource region. If not defined, the provider region is used.
                      The resource region. If not defined, the provider region is used.
                    type: string
                  remoteSecurityGroupId:
                    description: |-
                      (String) The remote security group which the rule should match.
                      The remote security group which the rule should match.
                    type: string
                  securityGroupId:
                    description: |-
                      (String) The security group ID.
                      The security group ID.
                    type: string
                  securityGroupRuleId:
                    description: |-
                      (String) The security group rule ID.
                      The security group rule ID.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
