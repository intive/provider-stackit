// SPDX-FileCopyrightText: 2024 The Crossplane Authors <https://crossplane.io>
//
// SPDX-License-Identifier: Apache-2.0

// Code generated by upjet. DO NOT EDIT.

package v1alpha1

import (
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	"k8s.io/apimachinery/pkg/runtime/schema"

	v1 "github.com/crossplane/crossplane-runtime/v2/apis/common/v1"
)

type ClusterBucketInitParameters struct {

	// (String) The bucket name. It must be DNS conform.
	// The bucket name. It must be DNS conform.
	Name *string `json:"name,omitempty" tf:"name,omitempty"`

	// (String) STACKIT Project ID to which the bucket is associated.
	// STACKIT Project ID to which the bucket is associated.
	ProjectID *string `json:"projectId,omitempty" tf:"project_id,omitempty"`

	// (String) The resource region. If not defined, the provider region is used.
	// The resource region. If not defined, the provider region is used.
	Region *string `json:"region,omitempty" tf:"region,omitempty"`
}

type ClusterBucketObservation struct {

	// It is structured as "project_id,region,name".
	ID *string `json:"id,omitempty" tf:"id,omitempty"`

	// (String) The bucket name. It must be DNS conform.
	// The bucket name. It must be DNS conform.
	Name *string `json:"name,omitempty" tf:"name,omitempty"`

	// (String) STACKIT Project ID to which the bucket is associated.
	// STACKIT Project ID to which the bucket is associated.
	ProjectID *string `json:"projectId,omitempty" tf:"project_id,omitempty"`

	// (String) The resource region. If not defined, the provider region is used.
	// The resource region. If not defined, the provider region is used.
	Region *string `json:"region,omitempty" tf:"region,omitempty"`

	// (String)
	URLPathStyle *string `json:"urlPathStyle,omitempty" tf:"url_path_style,omitempty"`

	// (String)
	URLVirtualHostedStyle *string `json:"urlVirtualHostedStyle,omitempty" tf:"url_virtual_hosted_style,omitempty"`
}

type ClusterBucketParameters struct {

	// (String) The bucket name. It must be DNS conform.
	// The bucket name. It must be DNS conform.
	// +kubebuilder:validation:Optional
	Name *string `json:"name,omitempty" tf:"name,omitempty"`

	// (String) STACKIT Project ID to which the bucket is associated.
	// STACKIT Project ID to which the bucket is associated.
	// +kubebuilder:validation:Optional
	ProjectID *string `json:"projectId,omitempty" tf:"project_id,omitempty"`

	// (String) The resource region. If not defined, the provider region is used.
	// The resource region. If not defined, the provider region is used.
	// +kubebuilder:validation:Optional
	Region *string `json:"region,omitempty" tf:"region,omitempty"`
}

// ClusterBucketSpec defines the desired state of ClusterBucket
type ClusterBucketSpec struct {
	v1.ResourceSpec `json:",inline"`
	ForProvider     ClusterBucketParameters `json:"forProvider"`
	// THIS IS A BETA FIELD. It will be honored
	// unless the Management Policies feature flag is disabled.
	// InitProvider holds the same fields as ForProvider, with the exception
	// of Identifier and other resource reference fields. The fields that are
	// in InitProvider are merged into ForProvider when the resource is created.
	// The same fields are also added to the terraform ignore_changes hook, to
	// avoid updating them after creation. This is useful for fields that are
	// required on creation, but we do not desire to update them after creation,
	// for example because of an external controller is managing them, like an
	// autoscaler.
	InitProvider ClusterBucketInitParameters `json:"initProvider,omitempty"`
}

// ClusterBucketStatus defines the observed state of ClusterBucket.
type ClusterBucketStatus struct {
	v1.ResourceStatus `json:",inline"`
	AtProvider        ClusterBucketObservation `json:"atProvider,omitempty"`
}

// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:storageversion

// ClusterBucket is the Schema for the ClusterBuckets API. ObjectStorage bucket resource schema. Must have a region specified in the provider configuration. If you are creating credentialsgroup and bucket resources simultaneously, please include the depends_on field so that they are created sequentially. This prevents errors from concurrent calls to the service enablement that is done in the background.
// +kubebuilder:printcolumn:name="SYNCED",type="string",JSONPath=".status.conditions[?(@.type=='Synced')].status"
// +kubebuilder:printcolumn:name="READY",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="EXTERNAL-NAME",type="string",JSONPath=".metadata.annotations.crossplane\\.io/external-name"
// +kubebuilder:printcolumn:name="AGE",type="date",JSONPath=".metadata.creationTimestamp"
// +kubebuilder:resource:scope=Cluster,categories={crossplane,managed,stackit}
type ClusterBucket struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`
	// +kubebuilder:validation:XValidation:rule="!('*' in self.managementPolicies || 'Create' in self.managementPolicies || 'Update' in self.managementPolicies) || has(self.forProvider.name) || (has(self.initProvider) && has(self.initProvider.name))",message="spec.forProvider.name is a required parameter"
	// +kubebuilder:validation:XValidation:rule="!('*' in self.managementPolicies || 'Create' in self.managementPolicies || 'Update' in self.managementPolicies) || has(self.forProvider.projectId) || (has(self.initProvider) && has(self.initProvider.projectId))",message="spec.forProvider.projectId is a required parameter"
	Spec   ClusterBucketSpec   `json:"spec"`
	Status ClusterBucketStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// ClusterBucketList contains a list of ClusterBuckets
type ClusterBucketList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []ClusterBucket `json:"items"`
}

// Repository type metadata.
var (
	ClusterBucket_Kind             = "ClusterBucket"
	ClusterBucket_GroupKind        = schema.GroupKind{Group: CRDGroup, Kind: ClusterBucket_Kind}.String()
	ClusterBucket_KindAPIVersion   = ClusterBucket_Kind + "." + CRDGroupVersion.String()
	ClusterBucket_GroupVersionKind = CRDGroupVersion.WithKind(ClusterBucket_Kind)
)

func init() {
	SchemeBuilder.Register(&ClusterBucket{}, &ClusterBucketList{})
}
